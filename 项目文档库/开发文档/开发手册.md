# 项目概述

qingfei-blog-fullstack（青飞的小站）是一个基于 React + TypeScript + Express + SQLite 的个人博客系统。

## 技术栈

| 类别 | 技术 |
|------|------|
| 前端语言 | TypeScript |
| 前端框架 | React 18 |
| 前端构建 | Webpack 5 |
| CSS 框架 | Tailwind CSS |
| 路由 | react-router-dom（Hash 模式） |
| 动画 | framer-motion |
| Markdown | react-markdown + rehype-raw + remark-gfm |
| 图标 | lucide-react + Font Awesome |
| 打包导出 | JSZip |
| 后端 | Express 4 |
| 数据库 | better-sqlite3 |
| 认证 | JWT + bcryptjs |
| 文件上传 | multer |
| 包管理器 | pnpm |

## 目录结构

```
├── src/                          # 前端源码
│   ├── components/               # UI 组件
│   │   ├── AdminLoginModal.tsx   # 弹窗登录组件
│   │   ├── ArchiveSidebar.tsx    # 归档侧边栏
│   │   ├── BackgroundSettings.tsx # 背景设置
│   │   ├── CategorySidebar.tsx   # 分类侧边栏
│   │   ├── ClickSoundProvider.tsx # 点击音效
│   │   ├── Footer.tsx            # 页脚（仅首页显示）
│   │   ├── MarkdownEditor.tsx    # Markdown 编辑器（含多媒体工具栏）
│   │   ├── MarkdownRenderer.tsx  # Markdown 渲染器（含代码块复制）
│   │   ├── MusicPlayer.tsx       # 音乐播放器
│   │   ├── PostCard.tsx          # 文章卡片（含分享按钮）
│   │   ├── ScrollToButtons.tsx   # 回到顶部/底部按钮（右侧垂直居中）
│   │   ├── SeasonalEffect.tsx    # 季节特效
│   │   └── TagCloud.tsx          # 标签云
│   ├── pages/                    # 页面组件
│   │   ├── Home.tsx              # 首页（三栏布局 2+8+2，导出按钮）
│   │   ├── PostDetail.tsx        # 文章详情（编辑/分享/点赞）
│   │   ├── AdminDashboard.tsx    # 文章管理后台（表格+分类列）
│   │   ├── AdminEdit.tsx         # 文章编辑/新建（分类选择+新建分类弹窗）
│   │   └── AdminLogin.tsx        # 登录页（弹窗登录后备）
│   ├── hooks/                    # 自定义 Hooks
│   ├── services/                 # API 服务（调用后端 /api）
│   │   └── api.ts                # 统一 API 封装（含 uploadImage）
│   ├── styles/                   # 样式文件
│   ├── App.tsx                   # 应用根组件（含 ConditionalFooter）
│   └── index.tsx                 # 入口文件
├── server/                       # 后端服务
│   ├── src/
│   │   ├── server.ts             # Express 入口（静态文件+API+uploads）
│   │   ├── db/
│   │   │   └── index.ts          # SQLite 数据库操作
│   │   ├── routes/
│   │   │   ├── posts.ts          # 文章 API
│   │   │   ├── auth.ts           # 认证 API
│   │   │   └── upload.ts         # 文件上传 API（multer，限图片 5MB）
│   │   └── middleware/
│   │       └── auth.ts           # JWT 中间件
│   ├── data/
│   │   └── blog.db               # SQLite 数据文件
│   ├── uploads/                  # 上传图片存储目录
│   ├── package.json
│   └── tsconfig.json
├── assets/                       # 静态资源（favicon: 陈放.jpg）
├── 项目文档库/                    # 项目文档
│   ├── 项目介绍/
│   ├── 开发文档/
│   └── 部署运维/
├── scripts/                      # Coze 预览/部署脚本
├── dist/                         # 前端生产构建产物
├── data/                         # SQLite 数据文件
├── webpack.config.js             # Webpack 配置（含 favicon）
├── package.json
└── .coze                         # Coze 项目配置
```

## 关键入口

- **前端开发**: `pnpm run dev`（webpack-dev-server，端口 3266）
- **前端构建**: `pnpm run build`（输出到 dist/）
- **后端开发**: `cd server && pnpm run dev`（ts-node，端口 3015）
- **后端构建**: `cd server && pnpm run build`（输出到 server/dist/）
- **类型检查**: `pnpm run typecheck`

## 运行与预览

### 架构

- 开发模式：前端 webpack-dev-server (3266) + 后端 Express (3015)，前端通过 proxy 访问 /api
- 预览/生产模式：后端 Express (5000) 同时提供 API 和前端静态文件，并托管 /uploads 目录

### 预览链路

- 根 `.coze`: `/workspace/projects/.coze`
- preview 端口: `5000`
- 预览 build: 安装前后端依赖 + 构建前端 dist/
- 预览 run: 启动后端服务（NODE_ENV=production, PORT=5000），后端托管 dist/ 静态文件和 uploads/ 上传文件

### 部署链路

- 部署 build: 安装前后端依赖 + 构建前端 dist/ + 构建后端 server/dist/
- 部署 run: 启动编译后的后端服务（NODE_ENV=production, PORT=5000）

## API 端点

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | /api/health | 健康检查 | 否 |
| POST | /api/auth/login | 登录 | 否 |
| GET | /api/posts | 获取所有文章 | 否 |
| GET | /api/posts/:id | 获取单篇文章 | 否 |
| POST | /api/posts | 创建文章 | Bearer Token |
| PUT | /api/posts/:id | 更新文章 | Bearer Token |
| DELETE | /api/posts/:id | 删除文章 | Bearer Token |
| GET | /api/posts/category/:category | 按分类获取 | 否 |
| GET | /api/posts/search/:keyword | 搜索文章 | 否 |
| GET | /api/posts/meta/categories | 获取分类列表 | 否 |
| GET | /api/posts/meta/tags | 获取标签列表 | 否 |
| GET | /api/posts/meta/archives | 获取归档 | 否 |
| POST | /api/upload | 上传图片（multipart/form-data，限 jpg/png/gif/webp，≤5MB） | Bearer Token |

## 功能列表

| 功能 | 说明 | 涉及文件 |
|------|------|----------|
| 弹窗登录 | 首页「写文章」按钮，未登录弹出登录框，登录后自动跳转编辑器 | AdminLoginModal.tsx, Home.tsx, api.ts |
| 文章分类 | 编辑页支持下拉选择分类或新建分类，管理页表格显示分类 Tag | AdminEdit.tsx, AdminDashboard.tsx |
| 代码块复制 | Markdown 渲染器在代码块上方显示语言标签和复制按钮 | MarkdownRenderer.tsx |
| 图片上传 | 编辑器工具栏支持本地上传图片，渲染器正确显示图片 | MarkdownEditor.tsx, upload.ts, api.ts |
| 多媒体插入 | 编辑器支持插入视频、音频、嵌入网页（外链） | MarkdownEditor.tsx, MarkdownRenderer.tsx |
| 一键导出 | 首页「导出」按钮，打包所有文章为 Markdown 压缩包下载 | Home.tsx |
| 文章分享 | 文章卡片和详情页支持复制文章标题+链接到剪贴板 | PostCard.tsx, PostDetail.tsx |
| 文章点赞 | 文章详情页底部点赞按钮 | PostDetail.tsx |
| 回到顶部/底部 | 全局悬浮按钮，右侧垂直居中 | ScrollToButtons.tsx |
| 导出文章 | 首页一键导出所有文章为 Markdown 压缩包 | Home.tsx |
| 容器宽度统一 | 首页/文章页/编辑页/管理页统一为 max-w-screen-2xl (1536px) | Home.tsx, PostDetail.tsx, AdminEdit.tsx, AdminDashboard.tsx |
| Footer 控制 | 仅首页显示 Footer，高度精简为 py-2 | Footer.tsx, App.tsx |
| Favicon | 网页标签图标使用陈放.jpg | webpack.config.js |

## 用户偏好与长期约束

- 仅使用 `pnpm` 管理依赖，禁止 `npm` 或 `yarn`
- 对外 HTTP 服务端口固定为 `5000`
- 预览/生产服务绑定到 `0.0.0.0`，禁止绑定到 `127.0.0.1` 或 `[::1]`
- 不使用 9000 端口
- 管理员默认密码: `52ywq1314..`（首次部署后应立即修改）
- 数据库文件位于 `server/data/blog.db`
- 上传文件存储在 `server/uploads/`，限制为图片类型（jpg/png/gif/webp），单文件 ≤5MB
- 四页容器宽度统一为 `max-w-screen-2xl`（1536px），首页三栏布局为 `grid-cols-12`（2+8+2）
- 代码提交同时推送到 GitHub 和 Gitee 两个仓库

## 常见问题和预防

1. **端口冲突**: 启动前会清理 5000 端口残留进程
2. **环境变量**: webpack.config.js 通过 `process.env.PORT` 和 `process.env.HOST` 读取端口和主机配置
3. **前端构建产物**: 生产构建输出到 `dist/` 目录
4. **better-sqlite3 编译**: pnpm v10 默认忽略 build scripts，安装时需要手动运行 `npx prebuild-install` 或配置 `onlyBuiltDependencies`
5. **JWT Secret**: 默认使用环境变量 `JWT_SECRET`，未设置时使用硬编码 fallback（生产环境务必设置）
6. **GitHub 网络不稳定**: 腾讯云访问 GitHub 频繁出现 GnuTLS recv error -110，已配置 Gitee 双仓库，服务器从 Gitee 拉取部署
7. **pnpm 版本**: 使用 pnpm 9.15.9，升级至 v10/v11 可能导致 node_modules 异常需回退
